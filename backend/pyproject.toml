[tool.ruff]
target-version = "py312"
line-length = 100

[tool.ruff.lint.per-file-ignores]
# Relationships do SQLAlchemy usam forward-refs em strings (resolvidos em runtime)
"app/models/*.py" = ["F821"]
